# Venus configuration
# max_users: maximum number of concurrent active Venus sessions
max_users: 1

# auth_timeout_seconds: timeout for pending approval requests and auth challenges
auth_timeout_seconds: 60
